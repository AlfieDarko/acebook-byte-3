console.log("here")
$(document).ready(function() {
  console.log("inside")
  $("a[id^='like_']").off('click').on('click', function() {
    var id = parseInt(this.id.replace("like_", ""), 10);
    var mySpan = $("#likes_span_" + id);
    console.log(mySpan)
    mySpan.html('<%= j(render partial: "likes/like", locals: {post: @post}) %>');
  });
});
